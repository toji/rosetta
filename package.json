{
  "name": "rosetta",
  "version": "0.0.1",
  "description": "A shader reference resource comparing shaders from multiple languages",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve --port=8081",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["webgpu", "webgl", "shaders"],
  "author": "bajones@google.com",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0"
  }
}
